# Phase 3B Infrastructure Dependencies
# Redis Orchestration, Config, Testing

# Core infrastructure
redis==5.0.1                  # Redis client for blackboard coordination
python-dotenv==1.0.0         # Environment variable loading

# Configuration & validation
pydantic==2.5.2               # Config validation and serialization
PyYAML==6.0.1                 # YAML parsing for fallback config

# Testing & async
pytest==7.4.3                 # Testing framework
pytest-asyncio==0.21.1        # Async test support
pytest-timeout==2.2.0         # Timeout handling for tests

# Development & debugging
python-json-logger==2.0.7     # Structured logging to JSON
colorlog==6.8.0               # Colored console logging
